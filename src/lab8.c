/*
 *	A program to demonstrate passing arrays
 *	C Programming laboratory 8
 */

#define ARRAY_SIZE	10

void change(int *array, int array_size)
{
	/* Put some code in here to make changes to the array */
}

int main(void)
{
	int array[ARRAY_SIZE];

	/* Perhaps set some values in the array */

	/* Make sure you call the change function */

	/* Display what has happened to the array in some way */

	return 0;
}